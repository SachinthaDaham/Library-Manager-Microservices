apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: library-system
  labels:
    app: auth-service
    component: auth
spec:
  type: ClusterIP
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
  selector:
    app: auth-service
