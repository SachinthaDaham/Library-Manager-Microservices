apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: library-system
  labels:
    app: frontend
    component: ui
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: frontend

