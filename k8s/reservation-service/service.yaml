apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: library-system
  labels:
    app: reservation-service
    component: reservations
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
  selector:
    app: reservation-service
